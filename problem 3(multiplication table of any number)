#include<stdio.h>
void table(int n);
void table(int n){
for(int i=1;i<=10;i++)
    printf("%d X %d = %d\n",n,i,i*n);
}
int main()
{
    int n;
    printf("enter number: ");
    scanf("%d",&n);
    printf("The table of %d is:\n", n);
     table(n);
    return 0;
}
